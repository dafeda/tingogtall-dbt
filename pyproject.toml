[project]
name = "tingogtall-dbt"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "dbt-postgres>=1.10.0",
    "pre-commit>=4.5.1",
    "sqlfluff>=4.1.0",
]
